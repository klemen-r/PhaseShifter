#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"
cd "$ROOT_DIR/BACKEND/phaseshifter-server"

if [ "$#" -eq 0 ]; then
  set -- --symbols NQ,ES --log-level info
fi

cargo run --release -- "$@"
